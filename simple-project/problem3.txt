ATM PIN SimulatorCreate a string variable correct_pin set to "1234".Create an integer variable attempts set to 3.Write a while loop that runs as long as attempts > 0.Inside the loop, simulate a user guess (you can hardcode a guess variable inside the loop for testing).If the guess matches correct_pin, print "Access Granted" and break the loop.If it doesn't match, subtract 1 from attempts and print "Incorrect. Attempts remaining: [attempts]".If the loop finishes and attempts is 0, print "Account Locked".
